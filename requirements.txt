# ── Core runtime dependencies ────────────────────────────────────────────────
pydantic>=2.10,<3
pydantic-settings>=2.0,<3
python-json-logger>=2.0,<3

# ── OCR and document processing ──────────────────────────────────────────────
# opencv-python-headless must come before paddleocr/paddlex to avoid
# pulling in the full OpenGL/X11 stack (libgl1 + Mesa).
opencv-python-headless>=4.8,<5
# paddlepaddle + paddleocr + paddlex are large (~3.5 GB installed).
# Tests that require Paddle must be guarded:
#   @pytest.mark.skipif(not os.getenv("PADDLE_AVAILABLE"), reason="paddle not installed")
pypdfium2>=4.30,<5
Pillow>=10.0,<12
paddlepaddle>=3.2,<4
paddleocr>=3.3,<4
paddlex[ocr]>=3.4,<4

# ── LLM / Embeddings ─────────────────────────────────────────────────────────
openai>=2.30,<3

# ── Optional: Chroma vector store ────────────────────────────────────────────
# Uncomment to use ChromaDB instead of the built-in JSON vector store.
# Also set PREFER_CHROMA=true in your .env.
# chromadb>=0.5,<1
