# Secrets — never commit
.env
*.pem

# Claude Code project instructions
CLAUDE.md

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
dist/
build/

# Training outputs & generated artifacts
output/
checkpoints/

# Local dataset cache (large files)
data/image/
data/text/
dataset/

# HuggingFace cache
.cache/

# Terraform — ignore state, secrets, and local working files only
aws_ec2/.terraform/
aws_ec2/terraform.tfstate
aws_ec2/terraform.tfstate.backup
aws_ec2/terraform.tfvars
aws_ec2/tfplan
aws_ec2/launch_when_ready.sh

# macOS
.DS_Store
