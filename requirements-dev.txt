# ── Development and testing dependencies ─────────────────────────────────────
# Does NOT include paddlepaddle/paddleocr/paddlex (too large for dev installs).
# Paddle-dependent tests are skipped automatically:
#   @pytest.mark.skipif(not os.getenv("PADDLE_AVAILABLE"), reason="paddle not installed")
# To run all tests including Paddle-dependent ones:
#   PADDLE_AVAILABLE=1 pytest

# Production dependencies (non-Paddle)
pydantic>=2.10,<3
pydantic-settings>=2.0,<3
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
python-multipart>=0.0.9,<1
python-json-logger>=2.0,<3
openai>=2.30,<3
pypdfium2>=4.30,<5
Pillow>=10.0,<12

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.0,<9
pytest-cov>=5.0,<6
pytest-asyncio>=0.23,<1
httpx>=0.27,<1
# pytest-watch: re-runs tests automatically when source files change
# Usage: ptw tests/unit/  or just: ptw
pytest-watch>=4.2,<5

# ── Linting and formatting ────────────────────────────────────────────────────
ruff>=0.4,<1
